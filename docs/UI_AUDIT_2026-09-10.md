# UI 全量审计报告（2026-09-10）

> 审计方式：x86_64 模拟器（Android 17，sdk_gphone16k_x86_64）安装 debug 包实机走查，
> 逐页截图存 `build/audit_shots/`；辅以代码静态检查（设计令牌遵守度、硬编码扫描）。
> 截图预览：`build/audit_shots/*.jpg`（01_launch ~ 10_settings）。

---

## 一、审计结论（总览）

整体 UI 质量良好：全 APP 统一使用 `AppColors/AppRadius/AppShadows/AppSpacing`
设计令牌（33 个页面仅 6 处硬编码颜色），渐变头部、卡片阴影、圆角体系一致，
无发现布局溢出。主要短板集中在 **图标体系、异常文案、版本号一致性** 三类。

## 二、问题清单

### P0 —— 上架前必须修（本轮已修复 ✅）

| # | 问题 | 位置 | 状态 |
|---|------|------|------|
| 1 | 版本号硬编码 `v1.0.0`，实际已 3.0.0（启动页 + 我的页页脚） | `splash_page.dart`、`profile_page.dart` | ✅ 统一改读 `AppConfig.appVersion` |
| 2 | AI 生成失败把原始异常暴露给用户（`ServiceException(429/1305)…`） | `discover_page.dart` | ✅ 映射为友好文案（限流/鉴权/网络/通用四类） |
| 3 | 首页「当前海拔」可能永久卡在「定位中…」（GPS 无超时兜底） | `app_providers.dart`、`home_page.dart` | ✅ 定位加 8s 超时 + UI 三态（定位中/暂不可用/数值） |
| 4 | 缺账号注销入口（小米等渠道审核强制） | `settings_page.dart`、`auth_service.dart` | ✅ 新增「注销账号」（两步确认，云端 DELETE /auth/account + 本地清库） |

### P1 —— 视觉美化重点（下一批）

| # | 问题 | 位置 | 建议 |
|---|------|------|------|
| 5 | **emoji 当图标**：快捷功能 8 项、发现页分类/精选卡、每日提示等，iOS/Android/各厂商 emoji 风格不一，无品牌感 | `home_page.dart`、`discover_page.dart` 等 | ✅ **已完成（本轮）**：全部界面图标位统一为 Material 线性图标（首页快捷网格 + 天气卡、发现页/收藏页/拍照识物/轨迹/万年历/一日游/引导页，天气图标经新增 `lib/core/ui/weather_icons.dart` 统一映射）。仅保留内容型 emoji（分享文案、节日名、穿衣建议原文） |
| 6 | 精选卡片头图用 emoji 占位 | `discover_page.dart` | Phase 7 接入网络图时统一换成图 + 缓存 |
| 7 | 设置页暴露接口约定等开发者信息（POST {地址}/api/v1/…） | `settings_page.dart` | 折叠进「高级选项」或仅在后端地址非空时显示 |

### P2 —— 体验打磨（后续批次）

| # | 问题 | 建议 |
|---|------|------|
| 8 | 引导页图标为 emoji（🗺️），品牌感弱 | 换 Logo/品牌插画 |
| 9 | 「关于」页去评分仅 SnackBar 提示 | 上架后按渠道跳对应市场 |
| 10 | 部分列表空态文案偏功能向 | 统一空态插画 + 引导按钮 |

## 三、已验证页面（截图走查通过）

- 启动页 / 隐私协议弹框：合规文案完整，不同意可退出 ✅
- 引导页四屏：布局正常，跳过/下一步可用 ✅
- 首页：天气大卡、提醒条、概览、附近探索、快捷功能、助手卡、节日倒计时均正常渲染 ✅
- 发现页：精选横滑、分类入口正常；AI 限流文案已修（P0-2）✅
- 行程页：三列统计、录制入口、万年历（农历/节气渲染正确）✅
- 我的页：登录卡、四列统计、功能列表、开关行均正常 ✅
- 天气详情：24 小时横滑、10 天预报、出行建议正常 ✅
- AI 聊天：空态/输入栏正常 ✅
- 设置页：账号/数据/同步分区正常 ✅

## 四、遗留事项
- 模拟器为一台 x86_64 AVD（伪装 OnePlus HD1910 属性）：debug 调试需
  `flutter build apk --debug --target-platform android-x64`；真机（arm64）用
  `--target-platform android-arm64`。**真机实测待用户连接物理设备后进行。**
- ~~服务端（MicroTripServer）暂无 `DELETE /api/v1/auth/account`~~
  → ✅ **已补齐（Java 后端 MicroTripServerJava）**：新增 `DELETE /api/v1/auth/account`
  （删除账号 + 云端轨迹 + 吊销当前令牌，单事务，`UserService.deleteAccount`），
  并补 2 个接口契约测试（令牌失效 + 账号不可再登录 / 无令牌 401）。
  注：旧 Node 后端（MicroTripServer）挂载路径无 `/api/v1` 前缀，客户端走新 Java 后端。

---

## 五、第二轮定向优化（2026-09-10）

模拟器走查反馈 4 处功能界面细节，已全部落地并回归：

| # | 反馈 | 处理 | 关键实现 |
|---|------|------|----------|
| 1 | 首页快捷功能图标调大；卡片内标题与宫格间距缩小 | ✅ | 图标底容器 48 → 按列宽自适应 46~58，图标本体 24 → 26~32；标题间距 6 → 2；宫格高度改用 `mainAxisExtent`（含字号缩放），替代固定 `childAspectRatio` |
| 2 | 发现页「为你精选」标题与卡片间距调大、「分类」调小 | ✅ | `SizedBox` 12 → 18（精选）/ 8 → 4（分类） |
| 3 | 首页天气卡内都可点击跳天气详情 | ✅ | `GradientHeader` 新增 `onCardTap`，以 `HitTestBehavior.opaque` 包住整卡；修复「点日期副标题/卡片留白无反应」（旧实现 `deferToChild` 只在子节点命中）；标题区（切城市）与刷新按钮层级更深仍各自生效 |
| 4 | 万年历标题栏月份箭头去掉、切换移到日历控件内；日期格头部不显示假日 | ✅ | 头部 actions 只留返回；卡片顶部新增「‹ 今天 ›」一行（主色浅底圆钮）；日期格右上角节日/休班胶囊整体移除，节日名由农历小标签与老黄历卡片承载 |

验证：

- `flutter analyze` → No issues；`flutter test` → **54 用例全绿**（新增 4 个回归用例）。
- 全路由溢出扫描（320~414 宽共 10 种尺寸，含 /home、/discover、/calendar）→ **0 处溢出**。
- 负向验证：临时注释首页 `onCardTap` 后，两个新增点击用例立即失败，证明用例有效。
- 截图：`build/audit_shots/` 下 `11~12`（首页）、`14~16`（发现/天气）等为第一轮；第二轮见本轮补充截图。

> 说明：日期格不再显示「休 / 班」角标是本次明确要求（避免与农历标签、黄历卡片三处重复）。
> 若后续希望恢复调休提示，可在农历标签行的颜色/角标上做二次设计。

